<?xml version="1.0" encoding="UTF-8"?>

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.0.xsd
        http://www.liquibase.org/xml/ns/dbchangelog-ext
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd
">

    <changeSet  id="1"  author="aaa">
       <sql>
               CREATE TABLE user (
                    id BIGINT NOT NULL AUTO_INCREMENT,
                    username VARCHAR(255) NOT NULL,
                    password VARCHAR(255) NOT NULL,
                    name VARCHAR(255) NOT NULL,
                    role VARCHAR(255) NOT NULL,
                    CONSTRAINT PK_id PRIMARY KEY (id)
                );
       </sql>
        <rollback>
            DROP table user;
        </rollback>
    </changeSet>
    <changeSet  id="2"  author="aaa">
        <sql>
            CREATE TABLE product (
            id BIGINT NOT NULL AUTO_INCREMENT,
            name VARCHAR(255) NOT NULL,
            price DOUBLE,
            explanation VARCHAR(1000),
            CONSTRAINT PK_id PRIMARY KEY (id)
            );
        </sql>
        <rollback>
            DROP table product;
        </rollback>
    </changeSet>
    <changeSet  id="3"  author="aaa">
        <sql>
            INSERT INTO product (name, price, explanation) values ("Product 1", 19.99, "Product is so expensive and has many attributes like X, Y, Z ...";
            INSERT INTO product (name, price, explanation) values ("Product 2", 29.99, "Product is so expensive and has many attributes like X, Y, Z ...";
            INSERT INTO product (name, price, explanation) values ("Product 3", 39.99, "Product is so expensive and has many attributes like X, Y, Z ...";
            INSERT INTO product (name, price, explanation) values ("Product 4", 49.99, "Product is so expensive and has many attributes like X, Y, Z ...";
            INSERT INTO product (name, price, explanation) values ("Product 5", 59.99, "Product is so expensive and has many attributes like X, Y, Z ...";
            INSERT INTO product (name, price, explanation) values ("Product 6", 69.99, "Product is so expensive and has many attributes like X, Y, Z ...";
            INSERT INTO product (name, price, explanation) values ("Product 7", 79.99, "Product is so expensive and has many attributes like X, Y, Z ...";
        </sql>
        <rollback>
            TRUNCATE table product;
        </rollback>
    </changeSet>
    <changeSet  id="4"  author="aaa">
        <sql>
            CREATE TABLE transaction (
            id BIGINT NOT NULL AUTO_INCREMENT,
            product_id BIGINT NOT NULL,
            user_id BIGINT NOT NULL,
            purchase_date DATETIME NOT NULL,
            CONSTRAINT PK_id PRIMARY KEY (id),
            CONSTRAINT fK_tran_product FOREIGN KEY (product_id) REFERENCES product(id) ON DELETE CASCADE ON UPDATE CASCADE,
            CONSTRAINT fK_tran_user FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE ON UPDATE CASCADE
            );
        </sql>
        <rollback>
            DROP table transaction;
        </rollback>
    </changeSet>
</databaseChangeLog>